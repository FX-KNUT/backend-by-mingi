<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.event.backend.mapper.ProductMapper">
    <select id="selectProductList" resultType="com.event.backend.dto.ProductDto">
        <![CDATA[
     	SELECT
     		brand,
     		event,
     		name,
     		price,
     		img
     	FROM
     		cvs
     	WHERE
     		brand = 'GS'
     ]]>
    </select>
</mapper>